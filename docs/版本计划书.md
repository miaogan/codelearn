# CodeLearn 版本计划书（V1.0 MVP 冲刺）

| 项目 | 内容 |
|------|------|
| 产品名称 | CodeLearn（编程语言学习平台） |
| 规划周期 | 3 个月（2026-09-28 ~ 2026-12-31） |
| 目标版本 | V1.0（MVP 正式发布） |
| 文档版本 | v1.0 |
| 编写日期 | 2026-09-25 |
| 关联文档 | [产品文档（PRD）](./产品文档.md) |

---

## 1. 版本目标

### 1.1 V1.0 一句话目标

> **验证「日常学习 → 单元考试 → 能力认证」三层闭环：让 100 个种子用户坚持学习 4 周以上，完成一次单元考试并获得可验证的电子证书。**

### 1.2 版本成功标准（可量化）

| 维度 | 指标 | V1.0 目标（发布后 30 天） |
|------|------|---------------------------|
| 体验闭环 | 注册用户完成「首课 → 完成 1 个单元 → 参加单元考试」的转化率 | ≥ 40% |
| 认证闭环 | 通过认证考试并获取证书的用户数 | ≥ 30 人（种子用户） |
| 留存 | D7 留存 | ≥ 25% |
| 习惯 | 周活跃用户中「≥3 天学习」占比（WAU3/WAU） | ≥ 35% |
| 质量 | 代码沙箱判题成功率（非用户代码错误导致） | ≥ 99% |
| 性能 | 页面首屏 < 2s；代码判题 P95 < 5s | 达标 |

### 1.3 版本范围

- **做**（对应 PRD P0）：日常学习闭环增强、单元考试、考试报告、能力图谱、认证考试、电子证书、排行榜基础版。
- **不做**（明确砍掉）：移动端原生 App、社区/UGC、B 端企业服务、广告、好友系统、成就徽章（P1 后置）。
- **技术债可接受项**：SQLite 单机部署（MVP 阶段）、证书电子版仅图片+编号验证（不接 CA）。

---

## 2. 现状盘点（开发起点）

| 能力 | 现状 | 缺口（V1.0 补齐） |
|------|------|--------------------|
| 账号体系 | 注册/登录/JWT | 无 |
| 学习路径 | 课程→单元→课时 解锁、种子数据（Go/Python 各 3 单元 8 课） | 单元与课时的「完成解锁」规则需与考试挂钩 |
| 习题 | 选择/填空/代码三题型、静态题源 | 无 |
| AI 能力 | Eino：习题生成、提示、自适应推荐、AI 导师（debug/chat/review/run）、RAG 问答、主观题 AI 判定 | 按知识点维度生成；生成质量校验 |
| 代码沙箱 | Go/Python 运行与判题 | 考试场景的资源限额与防滥用 |
| 游戏化 | XP、streak、hearts、每日目标（字段级） | 打卡引导 UI、学习日历、排行榜 |
| 考试 | 基础考试模式（AI 随机出题、批量提交、不扣心数、返回 score） | 单元组卷、计时、考试报告、重考 |
| 错题本 | 收录/标记已掌握/统计 | 考试错题自动回流 |

---

## 3. 版本规划总览

```
2026-09-28                   2026-10-25         2026-11-22        2026-12-31
     │  Sprint 0    Sprint 1    │  Sprint 2    Sprint 3  │ Sprint 4  Sprint 5  │ 发布
     │  准备/架构   日常学习闭环  │   单元考试    考试报告+    │ 能力认证    优化打磨  │ V1.0
     │  (2周)       (2周)        │   (2周)      自适应     │ +证书      (2周)     │ 12-28 灰度
     │                          │              (2周)      │  (2周)               │ 12-31 全量
     ▼                          ▼                        ▼                      ▼
     M0：基线                     M1：日常学习闭环可用        M2：考试闭环可用          M3：V1.0 发布
```

### 3.1 里程碑定义

| 里程碑 | 时间 | 达成条件（DoD） |
|--------|------|-----------------|
| **M0 基线** | 09-28 ~ 10-11（Sprint 0） | 数据模型扩展完成；CI 建立；前后端测试基线通过；无 LLM 降级模式验证 |
| **M1 日常学习闭环** | 10-12 ~ 10-25（Sprint 1） | 每日目标/打卡/学习日历/排行榜可用；用户可完成「首课→单元」全流程；D1 引导链路完整 |
| **M2 考试闭环** | 10-26 ~ 11-22（Sprint 2-3） | 单元考试（组卷/计时/判分）、考试报告、错题回流、自适应推荐联动全部可用 |
| **M3 认证闭环 + V1.0 发布** | 11-23 ~ 12-31（Sprint 4-5 + 发布窗口） | 能力图谱、认证考试、电子证书（编号+防伪码+验证页）上线；灰度→全量发布；达成 §1.2 成功标准 |

### 3.2 推进状态记录

> 记录实际开发进度，随迭代持续更新。

| 版本/里程碑 | 状态 | 完成时间 | 说明 |
|-------------|------|----------|------|
| **Sprint 0（架构基座）** | ✅ 已完成 | 2026-09-25 | 详见下方清单 |
| **Sprint 1（日常学习闭环）** | ✅ 已完成 | 2026-09-25 | 详见下方清单 |
| **Sprint 2-3（考试闭环）** | ✅ 已完成 | 2026-09-25 | 详见下方清单 |
| **Sprint 4（能力认证与证书）** | ✅ 已完成 | 2026-09-25 | 详见下方清单 |
| Sprint 5（打磨、性能与发布准备） | ✅ 已完成 | 2026-09-25 | 详见下方清单 |
| Sprint 6（竞争力需求包） | ✅ 已交付（第一批 U3/U4/U12/U13 + 第二批 U9/U10/U7/U5/U2） | 2026-09-25 | 详见下方清单 |

**Sprint 0 实际交付清单（2026-09-25）：**
- 数据模型：`Unit` 增加 `exam_duration_min`/`exam_question_count`；新增 `XPEvent`（XP 流水）、`Exam`、`ExamQuestion`（试卷快照）、`ExamSubmission`、`Certificate` 五张表
- 考试组卷服务 `ExamService`：按单元抽题（客观题优先、代码题 ≤30%）、单元课时完成前置校验（未完成返回 403）、批量判分（客观题比对 + 代码题沙箱判题）、错题自动回流错题本、考试报告
- 排行榜/学习日历 `LeaderboardService`：XP 流水（课时/练习/考试），自然周排行榜 + 我的排名，月度学习日历；练习 XP 每日上限防刷（10 次/天）
- 证书服务 `CertificateService`：不可枚举证书编号 `CL-YYYYMMDD-XXXX`、等级映射（beginner/intermediate/advanced）、在线验证
- 新 API：`POST /units/:id/exam`、`POST /exams/:id/submit`、`GET /exams/:id/report`、`GET /leaderboard`、`GET /users/me/calendar`、`GET /certificates`、`POST /certificates/verify`（公开）
- 质量验证：`go build`/`go vet` 通过；新增 20 个单元测试全绿（含判分/组卷比例/防刷/证书防伪）；端到端冒烟测试通过（组卷→交卷 75 分→排行榜 XP 60→日历→错题回流→伪造证书验证失败）
- 无 LLM 降级：考试组卷完全基于静态题库，不依赖 LLM（已验证）

**Sprint 1 实际交付清单（2026-09-25）：**
- 后端：`PUT /users/me/daily-goal` 每日目标设置接口（数值钳制 [10,500] 并回显实际值，已修复回显 bug）
- 首页改造 [HomeView](../frontend/src/views/HomeView.vue)：今日目标 SVG 进度环（today_xp/daily_goal）、达成态切换、🔥 连续打卡徽章、每日目标快捷设置（20/50/100 XP）、「继续学习」直达首个未完成课时、错题巩固推荐（今日推荐 3 题，无 LLM 依赖）、排行榜入口
- 排行榜页 [LeaderboardView](../frontend/src/views/LeaderboardView.vue)：我的排名卡片 + 周榜（前三奖牌样式）+ 空态引导
- 学习日历 [ProfileView](../frontend/src/views/ProfileView.vue)：月度 GitHub 风格热力图（按当日 XP 三档着色），沿用月份切换由后端 `month` 参数支持
- 路径页 [PathView](../frontend/src/views/PathView.vue)：每个单元末尾新增「单元考试」节点，全部课时完成后解锁（渐变紫色节点 + 呼吸光效），点击进入考试
- 单元考试页 [UnitExamView](../frontend/src/views/UnitExamView.vue)：考试说明 → 倒计时答题（选择题/填空题/代码题三种作答器）→ 交卷 → 成绩报告（通过/未通过、逐题解析、错题引导）
- 顶栏 [StatsBar](../frontend/src/components/StatsBar.vue)：新增 🏆 排行榜入口
- 新路由：`/leaderboard`、`/unit-exam/:unitId`
- 质量验证：`go build`/`go vet`/`go test` 全绿；`vue-tsc` 类型检查通过；`npm run build` 构建成功；端到端冒烟通过（每日目标钳制 500、考试 4 题交卷 25 分未通过无考试 XP、排行榜/日历正确）

**Sprint 2-3 实际交付清单（2026-09-25）：**
- 重考规则：`ExamService.SubmitExam` 增加冷却拦截（未通过 24h 内重考返回 403「冷却中」）与已通过拦截（`ErrAlreadyPassed`）；`ExamSubmission` 记录 `attempts`/`best_score` 取最高分
- 防作弊：`ExamSubmission` 新增 `TabSwitches` 字段，提交接口 `POST /exams/:id/submit` 接收切屏次数；[UnitExamView](../frontend/src/views/UnitExamView.vue) 页面失焦检测（≥3 次弹警告、提交时上报），报告页展示切屏记录
- 考试报告增强：报告新增分题型正确率 `by_type`（选择/填空/代码条形图）与知识点掌握度 `knowledge`（列表展示掌握状态），前端 [UnitExamView](../frontend/src/views/UnitExamView.vue) 报告区块渲染
- 能力图谱 v1：`SkillService.CourseSkillMap` 按课时（知识点）聚合练习正确率/错题/考试通过数据，权重计算（completed=50 / practice=40 / exam=10 / 错题惩罚=25），状态阈值 ≥70 已掌握、<40 薄弱；新页面 [SkillMapView](../frontend/src/views/SkillMapView.vue)（按单元分组 + 总体环形进度 + 状态图例）；路径页「能力图谱」入口
- 自适应推荐联动：`GET /users/me/review-recommendations` 聚合薄弱章节复习建议；[HomeView](../frontend/src/views/HomeView.vue) 接入「薄弱章节复习推荐」区（绿色边框样式）
- 错题回流来源标记：`WrongExercise` 新增 `Source`（exercise/exam），错题列表接口已暴露来源字段
- 新 API：`GET /courses/:id/skill-map`、`GET /users/me/review-recommendations`
- 质量验证：`go build`/`go vet`/`go test` 全绿（新增重考冷却/已通过拦截/能力图谱状态等测试）；`vue-tsc` 通过；`npm run build` 成功；端到端冒烟通过（交卷 tab_switches=3 → 重考被 403 拦截 → 错题 source=exam → 报告含 best_score/tab_switches/by_type/knowledge → 能力图谱区分薄弱/未学 → 复习推荐正确聚合）
- 说明：Sprint 2 原计划的「题目/选项乱序」未单独实现（组卷时已随机洗牌）；Sprint 3 原计划的「分数预测 v1」为 P1 可选，留待后续迭代

**Sprint 4 实际交付清单（2026-09-25）：**
- 认证考试：`ExamService.AssembleCertExam` 课程级综合考试（覆盖全部单元，每单元至少 1 题 + 30% 代码题上限，45 分钟 10 题）；触发条件 = 课程全部单元考试通过（未满足返回 403）；复用提交/判分/报告/错题回流全链路
- 认证状态接口：`GET /courses/:id/cert-status` 返回 `eligible`/`certified`/`units_passed` 等，驱动前端入口解锁
- 发证联动：认证考试通过（≥60 分）→ 自动颁发证书；`ExamReportDTO` 新增 `exam_type`/`course_id`/`certificate` 字段，报告页展示「🎖️ 证书已颁发」区块
- 能力等级划分：复用 `CertificateService.LevelForScore`（≥80 熟练 / ≥60 进阶 / 其余入门），已通过单元考试测试覆盖
- 证书在线验证页 [VerifyView](../frontend/src/views/VerifyView.vue)：公开路由 `/verify`（无需登录），输入编号验证真伪并展示证书详情（持有人/课程/等级/成绩/编号/日期），支持 `?no=` 预填
- 我的证书 [MyCertificatesView](../frontend/src/views/MyCertificatesView.vue)：`/certificates` 列表页（等级徽章：入门绿/进阶蓝/熟练橙），空态引导，查看证书/验证入口
- 证书展示页 [CertificateDetailView](../frontend/src/views/CertificateDetailView.vue)：`/certificate/:no` 证书卡片（金色边框/等级徽章/编号），防伪二维码（qrcode 库，编码公开验证 URL），「生成分享图」Canvas 绘制并下载 PNG
- 认证考试入口：路径页 [PathView](../frontend/src/views/PathView.vue) 新增金色「课程认证考试」节点（未解锁显示进度 x/3，已持证显示等级）；[UnitExamView](../frontend/src/views/UnitExamView.vue) 双模式复用（`/cert-exam/:courseId`）
- 入口导航：[ProfileView](../frontend/src/views/ProfileView.vue) 新增「🎖️ 我的证书」入口
- 新 API：`POST /courses/:id/cert-exam`、`GET /courses/:id/cert-status`
- 质量验证：`go build`/`go vet`/`go test` 全绿（新增认证考试资格校验/覆盖全单元组卷/认证状态/代码题 30% 上限等测试）；`vue-tsc` 通过；`npm run build` 成功；端到端冒烟通过（完成 8 课时 → 通过 3 个单元考试 → cert-status eligible → 认证考试 100 分通过 → 自动颁发 advanced 级证书 → 公开验证真实编号 valid=true / 伪造编号 valid=false → 我的证书列表 1 张 → 未达标用户 403 拦截）

**Sprint 5 实际交付清单（2026-09-25）：**
- **XP 口径修复**（浏览器体验发现 bug）：`RecordExerciseXP`/`RecordExamXP` 改为返回实际发放 XP 并同步累加 `user.XP`（`addUserXP`），`CompleteLesson` 同口径；新增启动对账 `RebuildUserXP`（`UPDATE users SET xp = SUM(xp_events.amount)`），保证顶栏 💎 与今日进度环同口径；测试新增口径断言
- **沙箱加固** [hardening.go](../backend/sandbox/hardening.go)：静态恶意代码检测（`rejectMalicious`，Go/Python 危险模式黑名单，`os/exec`、`socket`、`os.system` 等）、按语言异常熔断（`circuitBreaker`，5 次失败开 30 秒，`openFor` 可配置）、`limitWrap` 资源限额（bash `ulimit`：CPU 8s / 文件 8MB / 进程 64；Go 用 `GOMEMLIMIT=256MiB`，Python 用 `ulimit -v 262144`）
- 修复 Go 沙箱资源限制 bug：`limitWrap` 原按 `argv[0]=="go"` 判断语言，编译后二进制被误判为 Python 加 `ulimit -v` 导致 Go runtime 内存预留失败 → 改为显式传 `language` 参数，Go 分支改用 `GOMEMLIMIT`；`go test ./sandbox/` 全绿（新增恶意检测/熔断开合恢复/引号转义测试）
- 合规补齐：[LegalView](../frontend/src/views/LegalView.vue)（用户协议/隐私政策/AI 生成标识说明，`/legal`）；[RegisterView](../frontend/src/views/RegisterView.vue) 注册需勾选《用户协议》《隐私政策》（未勾选禁用提交并提示）；[LessonView](../frontend/src/views/LessonView.vue) 练习问答题展示「AI 生成」徽章
- 埋点与看板：`AnalyticsEvent` 表四类事件（register/lesson_complete/unit_exam_passed/cert_issued），注册/完成课时/单元考试通过/发证成功时记录；管理端 `GET /api/admin/funnel?days=30` 输出学习漏斗（按去重用户数），需 `X-Admin-Token`（`ADMIN_TOKEN` 环境变量，未配置返回 404）
- 用户反馈入口：[FeedbackView](../frontend/src/views/FeedbackView.vue)（类别选择 + 内容 + 联系方式，`/feedback`）；`POST /api/feedback` 落库 `UserFeedback`（空内容/纯空白返回 400）；[ProfileView](../frontend/src/views/ProfileView.vue)「📮 问题反馈」入口
- 种子内容扩充：Go 课程 +6 题（bool 关键字/fmt.Println/switch 逗号/func 关键字/len/声明 map）、Python 课程 +6 题（True/False/字符串拼接/continue/pass/pop/元组不可变），服务端组卷题库覆盖更广
- 新 API：`GET /api/admin/funnel`（管理端）、`POST /api/feedback`
- 质量验证：`go build`/`go vet`/`go test ./...` 全绿（新增 `analytics_service_test.go`：漏斗顺序/去重/默认窗口/反馈空白校验；`progress_service_test.go`/`auth_handler_test.go` 迁移新表并适配构造器）；`vue-tsc` 通过；`npm run build` 成功；端到端冒烟 42/42 通过（注册→8 课时完成 XP=160→练习 XP=260→Go/Python 沙箱判题 2/2→恶意代码拦截→3 个单元考试通过→cert-status eligible→认证考试 70 分通过→证书下发 CL-20260925-XXXX→证书在线验证 valid=true→漏斗 register=1/lesson_complete=1/unit_exam_passed=1/cert_issued=1→反馈 201/空白 400→user.XP=340==流水和==stats）
- 全链路性能压测（2026-09-25，3 核 5.8G 沙箱环境实测）：
  - 代码沙箱判题 P95：Go（含编译）**534ms**、Python **1238ms**（目标 <5s ✅）
  - 考试批量提交 P95：开考组卷 **10ms**、考试交卷 **469ms**、批量判分接口 **640ms**（目标 <3s ✅）
  - 核心读接口并发（20 并发 ×60）P95：courses 11ms / 学习路径 28ms / stats 18ms / 排行榜 18ms / 日历 12ms / 能力图谱 119ms / 证书 8ms / 复习推荐 64ms
  - 写接口并发（8 并发 ×24）P95：注册 415ms（SQLite 写串行化）、反馈 135ms
  - 前端首屏（真实 Chromium 冷缓存，Playwright 实测）P95：登录页 FCP **420ms** / 导航至空闲 **559ms** / TTFB 6ms / DCL 51ms / Load 55ms（目标 <2s ✅）；登录态首页 FCP **92ms**、导航至空闲（含 8 个 API 瀑布，单接口 4-12ms）**641ms**，12 个请求 / 187KB
  - 说明：Sprint 5 原计划的「全链路性能压测」已按验收标准完成（沙箱 <5s、考试提交 <3s、页面首屏 <2s 全部达标且余量充足）；压测基座（熔断/限额/超时）与工具链（Python 并发压测 + Playwright 浏览器测速）已就绪，灰度窗口可复用

**Sprint 6 实际交付清单（第一批 · 快赢项 U3/U4/U12/U13，2026-09-25）：**
- **U4 补签卡 + streak 保护**：`User` 新增 `FreezeCards`（新用户默认 2，持有上限 5）；`ProgressService.updateStreak` 断签保护（间隔 ≥2 天且持有补签卡 → 消耗 1 张保持 streak，否则重置为 1）；通过单元/认证考试奖励 1 张（`AwardFreezeCard`，上限 5）；`GET /users/me/stats` 暴露 `freeze_cards`；顶栏 [StatsBar](../frontend/src/components/StatsBar.vue) 新增 🧊 补签卡展示
- **U3 间隔重复复习（SRS）**：`WrongExercise` 新增 `ReviewStage`/`NextReviewAt`（`UpsertWrongExercise` 新错题立即到期并重置阶段）；复习间隔 [1,2,4,7,15] 天，`SRSReviewService` 提供今日到期清单与提交复习（答对升阶并拉长间隔、达最高级自动掌握；答错重置 0 且 1 天后重试）；新 API `GET /users/me/srs/reviews`、`POST /wrong-exercises/:id/srs-review`；新页面 [SRSReviewView](../frontend/src/views/SRSReviewView.vue)（作答器 + 😎记住了/😵忘记了 + 熟练度进度条）；首页「今日复习」入口（展示待复习数）
- **U12 成就徽章 + 段位称号**：`Achievement` 表 + 8 枚徽章（初出茅庐/七日之约/月度战士/首战告捷/满分学霸/能力认证/勤学苦练/千锤百炼）+ XP 段位称号（代码萌芽→代码学徒→代码骑士→代码剑士→代码大师→代码宗师，阈值 0/100/300/600/1000/2000）；`AchievementService` 事件驱动幂等解锁（完成课时/考试通过/发证/练习答对）；新 API `GET /users/me/achievements`；新页面 [AchievementsView](../frontend/src/views/AchievementsView.vue)（段位卡片 + 升级进度条 + 徽章墙锁定态）；首页「成就墙」入口
- **U13 学习周报**：`WeeklyReportService` 统计过去 7 天（XP/活跃天数/完成课时/练习数/通过考试/新增错题/连续天数）+ 薄弱知识点 TOP3 + 模板化总结与建议（纯模板生成、无 LLM 依赖可降级，后续可接 Eino）；新 API `GET /users/me/weekly-report`；新页面 [WeeklyReportView](../frontend/src/views/WeeklyReportView.vue)（周报汇总卡 + 统计格 + 薄弱点列表 + 建议）；首页「学习周报」入口
- 入口导航：[ProfileView](../frontend/src/views/ProfileView.vue) 新增 🏆 成就 / 🧠 复习 / 📊 周报 入口；首页新增三格快捷入口条（今日复习/学习周报/成就墙）
- 质量验证：`go build`/`go vet`/`go test ./...` 全绿（新增 `srs_service_test.go` 4 个、`achievement_service_test.go` 7 个、`weekly_report_service_test.go` 3 个、`progress_service_test.go` 补签卡保护/上限 2 个）；`vue-tsc` + `npm run build` 成功；端到端冒烟通过（注册得 2 张补签卡 → 首页快捷入口渲染 → 完成课时 +40XP → 成就墙「初出茅庐」解锁/段位「代码萌芽」Lv.1/距下段位 60XP → 故意答错 → 今日复习 1 题（熟练度 0/5）→ 答对提交「记住了」→ 清单清空（stage=1、next_review_at=+1 天）→ API 提交「忘记了」→ 重置 stage=0/+1 天重试 → 周报汇总正确（40XP/1 课时/3 题/1 错题/薄弱点 Hello World）→ 5 个新页面/改版页面无 console 错误）
- 说明：Sprint 6 快赢项中 **U2（PWA 移动端基础）** 与护城河项（U9 项目实战工坊 / U10 数字能力档案 / U7 分数预测 / U5 AI 学习计划）按 §4 计划进入第二批推进

**Sprint 6 实际交付清单（第二批 · 护城河项 + PWA，2026-09-25）：**
- **U9 项目实战工坊（多文件沙箱）**：`Project`/`ProjectFile` 模型（`AutoMigrate` 注册）；[sandbox/project.go](../backend/sandbox/project.go) 多文件运行（临时目录落盘 → 拒绝 `..`/`/` 文件名 → `limitWrap` 限额 → 熔断/超时复用，Python `python main.py` / Go `go run .`）；[project_service.go](../backend/service/project_service.go) 提供模板（Python/Go 默认文件）+ 创建/列表/详情/保存文件/运行/完成/删除；完成项目联动成就徽章（[achievement_service.go](../backend/service/achievement_service.go) 新增 🛠️ 项目实战 `first_project`、🏗️ 项目达人 `project_3`）；新 API `GET/POST /api/projects`、`GET /api/projects/:id`、`PUT /api/projects/:id/files`、`POST /api/projects/:id/run|complete`、`DELETE /api/projects/:id`、`GET /api/projects/templates`；新页面 [ProjectWorkshopView](../frontend/src/views/ProjectWorkshopView.vue)（项目列表 + 新建表单 + 多文件编辑器 + 运行输出面板 + 完成/删除）；首页「项目工坊」快捷入口 + 个人页入口
- **U10 可验证数字能力档案**：[portfolio_service.go](../backend/service/portfolio_service.go) 聚合公开数据（XP/连续天数/段位/徽章数 + 各课程单元通过率、掌握度 = 单元通过率×70% + 最近考试正确率×30%、认证等级与证书编号、完成项目数 + 全部项目作品）；新公开 API `GET /api/me/:username`（无鉴权，不暴露隐私）；新页面 [PortfolioView](../frontend/src/views/PortfolioView.vue)（能力卡 + 课程掌握度条 + 项目作品墙 + 分享链接复制），支持 `/portfolio/:username` 公开分享路由；个人页「能力档案」入口
- **U7 分数预测 + 考试日历**：[prediction_service.go](../backend/service/prediction_service.go) 四因子加权预测（练习正确率 35% + 错题掌握率 20% + 单元考试成绩 30% + 课时完成度 15%），无数据中性默认 50；level 阈值 low<50/medium<75/high；`Eligible` 全部单元考试通过；备考任务按未通过单元自动生成；新 API `GET /api/courses/:id/prediction`；新页面 [PredictionView](../frontend/src/views/PredictionView.vue)（概率环 + 因子权重条 + 备考建议 + 备考任务跳转考试）；课程页「考试预测」入口
- **U5 AI 学习计划生成**：[eino/plan.go](../backend/eino/plan.go) `PlanAdvisor`（OpenAI ChatModel 生成每周主题 + 5 条任务的 JSON 周计划，`Enabled()` 检测 LLM Key）；[study_plan_service.go](../backend/service/study_plan_service.go) LLM 优先、无 Key/失败自动模板降级（按周循环 12 个知识点主题，学习-练习-复习闭环）；新 API `POST /api/study-plan/generate`（goal/language/weeks 参数，weeks 收敛 1-12）；新页面 [StudyPlanView](../frontend/src/views/StudyPlanView.vue)（目标表单 + 周期选择 + 周计划卡片）；首页「AI 学习计划」快捷入口 + 个人页入口
- **U2 PWA 移动端基础**：`manifest.webmanifest` + SVG 图标（可安装）；[sw.js](../frontend/public/sw.js) Service Worker（静态资源缓存优先 + 导航网络优先离线回退首页 + `notificationclick`/`push` 预留）；[PwaBanner](../frontend/src/components/PwaBanner.vue) 安装引导 + 每日打卡提醒（Notification API，打开应用时按天提醒一次）；`index.html` 补充 theme-color / apple-mobile-web-app meta；`main.ts` 生产环境注册 SW
- 入口导航：[HomeView](../frontend/src/views/HomeView.vue) 快捷入口条扩展为 5 格（新增 🛠️ 项目工坊 / 🤖 AI 学习计划）；[ProfileView](../frontend/src/views/ProfileView.vue) 新增 🛠️ 项目工坊 / 📇 能力档案 / 🤖 学习计划；[PathView](../frontend/src/views/PathView.vue) action-bar 新增 🎯 考试预测（4 按钮，移动端自动换行）
- 质量验证：`vue-tsc` + `npm run build` 成功（四个新视图 + PWA 静态资源均产出到 dist）；后端 `go build`/`go vet`/`go test ./...` 全绿（见下方验证记录）


---

## 4. Sprint 详细计划

> 节奏：**双周迭代**；每个 Sprint 产出可演示的功能增量；每周一次演示，Sprint 结束做复盘。

### Sprint 0（09-28 ~ 10-11）：准备与架构基座

**目标**：为三层闭环打地基，明确数据模型与工程基线。

**交付物**：
- 数据模型扩展：Unit/Lesson/Exercise 增加 `exam_config`（考试组卷配置）；新增 `Exam`、`ExamSubmission`、`Certificate`、`SkillMap`、`Leaderboard` 表
- 考试组卷服务设计：按单元知识点抽题（客观题比例 70% + 代码题 30%）
- 排行榜设计：周结算 + 段位（青铜→白银→黄金→钻石），防刷（XP 获取速率限制）
- CI/CD 建立：后端 `go test` + 前端 `vue-tsc` + 构建产物上传
- 无 LLM 降级模式：Eino 调用失败回退静态题库，全链路验证
- 证书数据结构：证书编号生成规则（不可枚举）、防伪码、验证页 API

**验收标准**：新表迁移成功；测试基线绿；降级模式演示通过。

### Sprint 1（10-12 ~ 10-25）：日常学习闭环（→ M1）

**目标**：用户从注册到完成第一个单元，全程「学得进去、停不下来」。

**交付物**：
- 首页改造：今日目标进度环、streak 展示、推荐「今日 3 题」（联动自适应推荐）、打卡按钮与激励文案
- 学习日历（本月视图 + streak 记录）
- 每日目标设置（XP 或题数两档，参考多邻国目标设计）
- 排行榜基础版：XP 周榜 + 段位升降
- 解锁规则：单元内课时全部完成才解锁单元考试入口
- 前端：响应式布局适配移动端浏览器（为 PWA 铺路）

**验收标准**：新注册用户引导漏斗「注册→完成首课→达成首日目标」转化率 ≥ 60%；打卡后 streak 正确 +1，中断规则正确。

### Sprint 2（10-26 ~ 11-08）：单元考试系统（→ M2 前半）

**目标**：把现有「AI 随机考试」升级为「单元维度系统考试」。

**交付物**：
- 单元考试：按单元知识点组卷（8–12 题，选择 40% + 填空 30% + 代码 30%），固定题源（静态题库优先 + AI 补充）
- 考试计时器（建议 15 分钟/10 题）+ 到时自动提交
- 防作弊基础版：切屏警告（页面失焦记录）、题目/选项乱序
- 考试不扣心数（沿用现有逻辑），提交批处理接口（已有 ExamSubmit 扩展：关联 exam_id、记录时长）
- 重考规则：未通过 24h 冷却后可重考，取最高分

**验收标准**：单元考试全流程（进入→答题→提交→结果）无断点；组卷不重复、难度分布可配；切屏被记录。

### Sprint 3（11-09 ~ 11-22）：考试报告与自适应联动（→ M2）

**目标**：让考试结果「可解释、可行动」，并与学习路径联动。

**交付物**：
- 考试报告页：总分、正确率、分题型正确率、知识点掌握度（列表/雷达图雏形）、每题解析回看
- 错题自动回流错题本（含来源标记「考试」）
- 能力图谱 v1：按知识点聚合练习+考试数据，展示「未学/学习中/已掌握/薄弱」
- 自适应推荐联动：根据考试薄弱知识点，推荐对应复习课时与习题（增强现有 adaptive/recommend）
- 分数预测 v1（可选，若时间不足则 P1）：基于历史练习正确率与考试表现的通过概率预估

**验收标准**：一次考试后，能力图谱正确更新；薄弱知识点出现在「今日推荐」；错题本出现对应错题。

### Sprint 4（11-23 ~ 12-06）：能力认证与证书（→ M3 前半）

**目标**：打通「学完 → 考出 → 拿证」的最后一环。

**交付物**：
- 认证考试：课程级综合考试（覆盖全部单元，理论 + 代码实操，限时 45–60 分钟），触发条件 = 课程全部单元考试通过
- 能力等级划分：入门 / 进阶 / 熟练（按认证考试得分段位映射）
- 电子证书：姓名、等级、课程、日期、唯一编号 + 防伪二维码；证书生成（前端 Canvas/后端图片均可，MVP 优先前端生成 + 后端存证）
- 证书在线验证页：输入编号即可验证真伪与证书详情（公开只读页）
- 「我的证书」列表 + 社交分享（生成分享图）

**验收标准**：通过认证的用户可生成证书；验证页可查真伪；伪造编号返回「无效」；证书数据落库可追溯。

### Sprint 5（12-07 ~ 12-20）：打磨、性能与发布准备（→ M3 后半）

**目标**：稳定性、性能、合规达标，具备发布条件。

**交付物**：
- 全链路性能压测：代码沙箱 P95 < 5s、考试批量提交 < 3s、页面首屏 < 2s
- 沙箱加固：资源限额（CPU/内存/时长）、恶意代码检测、异常熔断
- 合规补齐：注册协议、隐私政策、AI 内容生成标识
- 种子内容扩充：Go/Python 课程补足单元考试与认证考试题库（静态题 + AI 生成人工抽检）
- 埋点与看板：注册/首课/单元完成/考试/认证漏斗埋点
- 用户反馈入口（问题上报）

**验收标准**：压测达标；安全自测通过；§1.2 成功标准指标全部可采集。

### Sprint 6（竞争力需求包 · V1.1 前瞻）

**背景**：V1.0 三层闭环（学-测-证）已交付并压测达标，但站在用户视角仍有三处竞争力缺口——**产出断点**（学完≠能被认可）、**场景断点**（手机碎片时间用不起来）、**坚持断层**（断签即弃、单人学习孤独）。本 Sprint 用「快赢项 + 差异化护城河」两批补齐，为 V1.1 商业化（订阅/认证收费）提供差异化能力。

**目标**：提升留存与信任，建立「编程 + 认证 + 项目作品」的差异化壁垒。

**交付物（快赢，P0，优先落地）**：
- **U3 间隔重复复习（SRS）**：错题/知识点按遗忘曲线排期「每日复习清单」，替代全量错题，首页与错题本联动（学完就忘的补丁）
- **U4 补签卡 + streak 保护**：断签可用补签卡恢复（学习获得/订阅赠送），降低「断了就不想碰」的流失
- **U12 成就徽章 + 段位称号**：首次课时/连续 7 天/首次考试/首次认证/项目完成等徽章墙；XP 等级称号（如 代码学徒→代码骑士）
- **U13 AI 学习周报**：每周自动总结（学了什么/薄弱点/下周建议），站内信 + 邮件召回流失用户
- **U2 PWA 移动端基础**：可安装、离线缓存、每日提醒推送，支撑地铁/通勤碎片化学习

**交付物（护城河，P1，差异化）**：
- **U9 项目实战工坊**：每个认证绑定 1-2 个真实项目（如「用 Python 自动生成周报」），沙箱升级「多文件项目模式」（建多文件/跑完整程序/看运行结果），完成项目方可领证——直击「光会语法不算会编程」
- **U10 可验证数字能力档案**：证书 + 项目作品 + 能力图谱合成公开能力页（`/me/:username`），招聘方扫码验证真伪——把「截图证书」升级为「可信数字凭证」，直击「HR 认不认」
- **U7 分数预测 + 考试日历**：基于练习/错题/模考数据预测认证考试通过概率（借鉴粉笔分数预测），备考倒计时 + 每日备考任务
- **U5 AI 学习计划生成**：用户输入目标（如「3 个月转数据分析」）→ AI 生成周计划并每日推进——「目标→计划」入口，多邻国/粉笔均无

**验收标准**：
- SRS：复习推荐次日点击率 ≥ 30%（对照全量错题列表）；补签卡使用率与断签后 7 日回流率可观测
- 项目工坊：完成项目→领证链路无断点；数字能力档案公开可访问且伪造编号验证失败
- 分数预测：对「认证考试通过/不通过」预测准确率 ≥ 75%（初版校准）
- PWA：可安装、离线打开可用、每日提醒可达
- 全链路回归：`go test ./...` / `vue-tsc` / `npm run build` 全绿；端到端冒烟通过；性能不劣化

### 发布窗口（12-21 ~ 12-31）

- **12-21 ~ 12-27**：内部灰度（团队 + 30 名种子用户），收集崩溃与体验反馈，修复 P0 缺陷
- **12-28**：全量发布 V1.0（线上公告 + 种子用户活动：认证考试限时免费）
- **12-31**：发布复盘，输出 V1.1 迭代清单

---

## 5. 功能与 Sprint 映射表

| PRD 编号 | 功能 | 优先级 | 归属 Sprint |
|----------|------|--------|-------------|
| F-L1 | 每日目标与打卡 | P0 | S1 |
| F-L2 | 技能树学习路径（解锁联动） | P0 | S1 |
| F-L3 | 多题型练习 | P0 | 已完成 |
| F-L4 | AI 习题生成（知识点维度） | P0 | S0-S1 |
| F-L5 | 错题本（考试回流） | P0 | S3 |
| F-L8 | 自适应推荐（考试联动） | P1→S3 | S3 |
| F-L9 | 学习日历与统计 | P1→S1 | S1 |
| F-L10 | 排行榜 | P1→S1 | S1 |
| F-E1 | 单元考试 | P0 | S2 |
| F-E3 | 考试报告 | P0 | S3 |
| F-E4 | 分数预测 | P1 | S3（可选）/P1 |
| F-E5 | 重考机制 | P1→S2 | S2 |
| F-C1 | 能力图谱 | P0 | S3 |
| F-C2 | 认证考试 | P0 | S4 |
| F-C3 | 电子证书 | P0 | S4 |
| F-C4 | 证书在线验证页 | P1→S4 | S4 |
| F-A2 | 个人中心与统计 | P0 | S1 |
| F-A3 | 移动端适配（响应式） | P1→S1 | S1 |
| F-G1 | XP/等级/心数 | P0 | 已完成 |
| — | 防作弊基础版（考试切屏/乱序） | P0 | S2 |
| — | 埋点与看板 | P0 | S5 |
| U3 | 间隔重复复习（SRS） | P0→S6 | S6 |
| U4 | 补签卡 + streak 保护 | P0→S6 | S6 |
| U12 | 成就徽章 + 段位称号 | P1→S6 | S6 |
| U13 | AI 学习周报 | P1→S6 | S6 |
| U2 | PWA 移动端基础 | P1→S6 | S6 |
| U9 | 项目实战工坊（多文件沙箱） | P1→S6 | S6 |
| U10 | 可验证数字能力档案 | P1→S6 | S6 |
| U7 | 分数预测 + 考试日历 | P1→S6 | S6 |
| U5 | AI 学习计划生成 | P1→S6 | S6 |
| U1 | 本地环境引导包 | P2 | 远期 |
| U6 | AI 苏格拉底式教练 | P2 | 远期 |
| U8 | 全真模考模式 | P2 | 远期 |
| U11 | 简历片段导出 | P2 | 远期 |

---

## 6. 团队与资源需求

### 6.1 建议团队（4–6 人）

| 角色 | 人数 | 主要职责 |
|------|------|----------|
| 产品经理 | 1 | 需求细化、验收、埋点口径 |
| 后端工程师 | 1–2 | Go 服务、考试/认证/排行榜服务、沙箱加固 |
| 前端工程师 | 1–2 | Vue3 页面、考试/报告/证书 UI、响应式 |
| 设计（可兼职/外包） | 0.5 | 打卡/排行榜/证书视觉、游戏化动效 |
| 测试/质量 | 1（可后端兼任） | 考试流程、沙箱安全、回归 |

### 6.2 外部依赖

- **LLM 服务**：LLM_API_KEY（豆包/OpenAI），需保证配额；无 Key 环境全功能降级
- **部署**：单机 Linux + SQLite（MVP）；预留 PostgreSQL 迁移路径
- **证书防伪**：自建编号 + 二维码校验（无需 CA 证书服务，降低复杂度）

---

## 7. 风险与依赖管理

| 风险 | 影响 | 应对 |
|------|------|------|
| 考试组卷与 AI 生成结合导致质量不稳 | 用户信任 | 静态题源优先（人工校验），AI 仅补充；代码题一律跑测试用例验证 |
| 沙箱安全/性能不过关（S5 才发现） | 发布延期 | S0 建立性能基线；S2 起每周沙箱压测；预留 1 周缓冲 |
| 证书设计返工（防伪/验证） | S4 延期 | S0 先定证书数据结构与编号规则；UI 与后端解耦 |
| LLM 服务不可用 | 体验降级 | S0 完成无 LLM 降级链路；生成失败重试与限流 |
| 3 个月内内容（题库）不足 | 考试可玩性低 | AI 生成 + 人工抽检滚动补齐；每 Sprint 完成 1 轮题库扩充 |
| 种子用户不足 | 无法验证闭环 | 发布前 2 周启动种子招募（开发者社区/高校社团）；给认证免费额度激励 |

---

## 8. V1.0 发布检查清单（Go/No-Go）

| 检查项 | 通过标准 |
|--------|----------|
| 三层闭环 | 新用户可无断点完成「首课→单元→单元考试→认证→证书」 |
| 稳定性 | 7 天无 P0 缺陷；沙箱成功率 ≥ 99% |
| 性能 | §1.2 性能指标达标 |
| 合规 | 隐私政策/用户协议上线；AI 内容标识 |
| 数据 | 全漏斗埋点可查；证书数据可追溯 |
| 降级 | 无 LLM Key 时全流程可用 |
| 安全 | 沙箱逃逸测试通过；证书编号不可枚举 |

---

## 9. 后续版本展望（V1.0 之后，供参考）

| 版本 | 主题 | 关键内容 |
|------|------|----------|
| V1.1（2027 Q1） | 留存强化 + 信任壁垒 | SRS 间隔复习、补签卡/streak 保护、成就徽章、AI 学习周报、PWA 移动端（即 Sprint 6 快赢项）；项目实战工坊 + 数字能力档案 + 分数预测进入正式版 |
| V1.2（2027 Q2） | 商业验证 | Pro 订阅上线（AI 导师/无限 AI 习题/深度报告）、认证考试收费；项目工坊作品与数字能力档案支撑企业背书 |
| V2.0（2027 下半年） | 规模化 | 新语言课程（JS/SQL）、社区/UGC、B 端企业测评与认证合作、PostgreSQL 集群化 |
| V2.x 远期 | 生态 | 认证联盟、内推/招聘对接（对标力扣/牛客 B 端链路） |

> 注：V1.1 及以后为展望，具体以 V1.0 发布后的数据与战略决策为准。
